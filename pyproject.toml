[project]
name = "de-cdc-data-pipeline"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "py4j>=0.10.9.7",
    "pyspark==3.5.0",
]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.uv]
managed = true
